# 환경 변수 템플릿 (git 추적 — 변수 이름만 공유, 실제 값은 비움)
# 실제 값은 .env.local 에 작성한다. .env.local 은 절대 커밋되지 않음.
#
# Vite는 VITE_ 접두사가 붙은 변수만 클라이언트 코드에 주입한다.
# 비밀(API Secret, DB 비밀번호 등)은 절대 VITE_ 변수에 넣지 말 것 — 빌드 결과에 평문으로 박힘.

# 백엔드 API 서버 주소 (예: http://localhost:8080)
VITE_API_BASE_URL=
