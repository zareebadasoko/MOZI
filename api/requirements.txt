# MOZI 챗봇 API 서버 의존성
#
# 설치:
#   pip install -r requirements.txt
#
# GPU 사용 시 torch 는 CUDA 빌드로 별도 설치 권장:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# (또는 시스템 CUDA 버전에 맞춰 cu121/cu118 등)

# --- 웹 서버 ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-dotenv>=1.0

# --- LLM (OpenAI) ---
openai>=1.40
langsmith>=0.1.50            # LANGSMITH_TRACING=true 일 때만 활성, 평소 no-op

# --- 검색 (Dense FAISS) ---
langchain-community>=0.2
langchain-openai>=0.1
faiss-cpu>=1.7               # GPU 쓰려면 faiss-gpu 로 교체

# --- 검색 (BM25 + 한국어 토큰화) ---
rank-bm25>=0.2.2
kiwipiepy>=0.18

# --- Reranker ---
sentence-transformers>=3.0
torch>=2.0                   # GPU 빌드는 위 안내 참고

# --- 공통 ---
numpy>=1.24
